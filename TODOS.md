# GitaGPT TODOs

Generated by `/autoplan` on 2026-04-15.

## Deferred Scope

- Add multi-translation comparison after v1 proves single-edition grounded answers.
- Add Sanskrit/transliteration mode only after extraction quality is verified.
- Add hosted auth and persisted conversation history after local v1 works.
- Add admin reindex endpoint only after token auth, rate limiting, and atomic index swap are implemented.
- Add corpus quality dashboard after v1 writes `corpus_report.json`.
- Add pgvector or another durable vector store only when multi-user or multi-document needs appear.
- Add formal deployment pipeline after local setup, ingestion, tests, and frontend are stable.

## Required Before Implementation

- Initialize git repo and add `.gitignore`.
- Keep source PDFs and generated indexes out of git by default.
- Frontend stack selected by user override: Next.js.
- Lock `/chat` response schema before frontend implementation.
- Generate inspected PDF extraction fixtures before parser implementation.


- Multi language support layer 
- Add:

“Daily Gita Insight” card on sidebar
(short 1-line wisdom)

👉 That makes users come back daily.
